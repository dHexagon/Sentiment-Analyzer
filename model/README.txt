pip install requirements.txt

Additional installations to be carried out :

  pip install git+https://github.com/openai/whisper.git - normal pip install whisper downloads a different module
  Note: 
    "pip install tokenizers" before you "pip install transformers"
    Ubuntu:
      sudo apt-get install ffmpeg
      sudo apt-get install portaudio19-dev
    Macos:
      brew install ffmpeg
      brew install portaudio19-dev


1. ML Models to be obtained by training the models using the given model codes.
1.1. 
